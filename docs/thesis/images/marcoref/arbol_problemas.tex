\begin{tikzpicture}[
    >=Latex,
    node distance=0.7cm,
    font=\small,
    bloque/.style={
        rectangle,
        draw,
        align=center,
        minimum height=1.15cm,
        text width=11.2cm,
        inner xsep=5pt,
        inner ysep=4pt
    },
    efecto/.style={
        rectangle,
        draw,
        align=center,
        minimum height=1.55cm,
        text width=4.8cm,
        inner xsep=5pt,
        inner ysep=4pt
    },
    problema/.style={
        bloque,
        minimum height=1.35cm,
        line width=0.9pt
    },
    causa/.style={
        rectangle,
        draw,
        align=center,
        minimum height=1.55cm,
        text width=3.35cm,
        inner xsep=4pt,
        inner ysep=4pt
    },
    subcausa/.style={
        rectangle,
        draw,
        align=center,
        minimum height=1.45cm,
        text width=3.35cm,
        inner xsep=4pt,
        inner ysep=4pt
    },
    flecha/.style={
        -{Latex[width=6pt,length=5pt]},
        semithick
    }
]

% PROBLEMA CENTRAL
\node[problema] (problema) {
    El operario dedica entre 53,2 y 66,6 s por ciclo\\
    a la dosificación manual de harina.
};

% EFECTOS
\node[efecto, above=1.1cm of problema.north west, anchor=south west] (efecto_observado) {
    Tiempo acumulado\\
    de dosificación:\\
    411,9 s en 7\\
    ciclos observados
};

\node[efecto, above=1.1cm of problema.north east, anchor=south east] (efecto_proyectado) {
    Tiempo mensual proyectado\\
    de dosificación:\\
    7\,128,6 s\\
    (1 h 58 min 48,6 s)
};

% CAUSAS DIRECTAS
\node[causa, below=1.15cm of problema] (c1) {
    Cargas y \\
    Traslados Sucesivos\\
};

\node[causa, below=1.15cm of problema.south west, anchor=north west] (c2) {
    Preparación y tara\\
    en cada ciclo
};

\node[causa, below=1.15cm of problema.south east, anchor=north east] (c3) {
    Ajuste y verificación\\
    final de la cantidad\\
    dosificada
};

% SUBCAUSAS
\node[subcausa, below=of c2] (sc1) {
    Separación entre\\
    almacenamiento y\\
    pesaje
};

\node[subcausa, below=of c1] (sc2) {
    3--4 cargas\\
    principales\\
    por ciclo
};

\node[subcausa, below=of c3] (sc3) {
    Retornos para\\
    cargas adicionales
};

% FLECHAS
\draw[flecha] (efecto_observado.south |- problema.north)
    -- (efecto_observado.south);
\draw[flecha] (efecto_proyectado.south |- problema.north)
    -- (efecto_proyectado.south);

\draw[flecha] (c2.north) -- (c2.north |- problema.south);
\draw[flecha] (c1.north) -- (c1.north |- problema.south);
\draw[flecha] (c3.north) -- (c3.north |- problema.south);

\coordinate (entrada_c1_izq) at ([xshift=-0.85cm]c1.south);
\coordinate (entrada_c1_der) at ([xshift=0.85cm]c1.south);
\coordinate (nivel_subcausas) at ([yshift=-0.35cm]c1.south);

\draw (sc1.north) -- (sc1.north |- nivel_subcausas)
    -- (entrada_c1_izq |- nivel_subcausas);
\draw[flecha] (entrada_c1_izq |- nivel_subcausas) -- (entrada_c1_izq);
\draw[flecha] (sc2.north) -- (c1.south);
\draw (sc3.north) -- (sc3.north |- nivel_subcausas)
    -- (entrada_c1_der |- nivel_subcausas);
\draw[flecha] (entrada_c1_der |- nivel_subcausas) -- (entrada_c1_der);

\end{tikzpicture}
